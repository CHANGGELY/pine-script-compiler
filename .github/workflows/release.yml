name: Release (skeleton)

on:
  workflow_dispatch:

jobs:
  prepare-release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20.x
          cache: 'npm'

      - name: Install dependencies
        run: npm ci || npm install

      - name: Placeholder for changesets/semantic-release
        run: |
          echo "Release workflow skeleton ready."
          echo "Integrate changesets or semantic-release when publishing is required."
